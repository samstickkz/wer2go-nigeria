# wer2 GO Nigeria — SEO change log

One line per autonomous SEO run (Mon/Wed/Fri). Newest at the bottom. This log
plus git history are the only memory between runs — read both before acting.

- 2026-08-03: Built `/drive` driver-recruitment page (backlog #1) — targets driver-intent queries ("drive for ride-hailing Nigeria", "driver app earnings") around the 10%-commission differentiator. Server-rendered with own metadata (title 50 chars, canonical /drive), JSON-LD WebPage + Service + BreadcrumbList + FAQPage (6 Q&A), 6 perks, 3-step how-to, city cross-links. Added to `app/sitemap.ts` (now 8 URLs). Repointed the Navbar "Drive" link from the external driver.gower2.com to internal `/drive`, and added a "How driving works" link from the homepage DriveWithUs section. Truth-checked: 10% commission / keep 90%, payouts ON REQUEST (no cadence), NIN+BVN onboarding, own hours, live cities from lib/cities — no invented earnings figures. `npm run build` clean; both JSON-LD blocks json-parse. Follow-up: homepage FAQ + FAQPage (backlog #2) is the next high-value gap; consider a Google Business Profile for the Maps pack.
- 2026-08-05: Built the homepage FAQ (backlog #2) — the homepage had zero FAQ content or FAQPage schema despite ride-hailing queries being heavily question-shaped. Added `lib/faqs.ts` (6 Q&A: fare-estimate honesty, driver verification, city coverage pulled live from `lib/site.ts` CITIES, the Kano-only Sannu women-only ride, the 10%-commission driver pitch, 24/7 multilingual support contact) and `components/Faq.tsx` (native `<details>` accordion, matches the `/drive` page's FAQ styling), rendered on `/` after DriveWithUs. `app/page.tsx` emits a standalone FAQPage JSON-LD block (mainEntity from the same data, so copy and schema can't drift). Added `#faq` to the Navbar. No new route, so `app/sitemap.ts` needed no change. Truth-checked against the codebase: no locked/guaranteed-fare wording, no payout-cadence promise, Sannu scoped to Kano only (verified in `lib/cities.ts`, not claimed platform-wide). `npm run build` clean; verified both JSON-LD blocks on `/` (`Organization+WebSite+Service+MobileApplication` graph, plus the new `FAQPage`) parse and the FAQ section/nav link render in the static HTML output. Follow-up: consider a Google Business Profile for the Maps pack; `lib/site.ts` SITE_DESCRIPTION still lists only 4 of the 5 live cities (omits Lagos) — worth a fix next run; several components (FareEstimator, EarningsCalculator, ReferralCallout, TopRides, Testimonials, PressLogos, BlogPosts) exist in `components/` but are not imported anywhere — dead code, candidate for cleanup or intentional revival.
- 2026-08-07: Technical-health fix (rotating away from the last two content-build runs) — `lib/site.ts` `SITE_DESCRIPTION` omitted Lagos (Nigeria's largest ride-hailing market, which has its own live `/lagos` page and is targeted twice in the homepage `keywords` array) from its "Live in ..." city list, and at 179 chars was already past Google's ~155-160 char SERP-snippet cutoff. This one string feeds the sitewide `<meta name="description">`, OG description, Twitter description, and the Organization + WebSite JSON-LD `description` fields, so the gap was live everywhere at once. Rewrote to "Book a ride with wer2 GO — verified drivers, upfront fare estimates and in-app SOS. Live in Lagos, Abuja, Port Harcourt, Uyo and Kano. Drivers keep 90%." (152 chars) — all 5 live cities present in the same order used by `lib/cities.ts`/`CitiesServed`, commission hook kept via the "keep 90%" framing already used on `/drive`. No other file hardcoded the old 4-city phrasing (grepped). `npm run build` clean; confirmed the new description string renders in the built `/` HTML and both JSON-LD blocks (Organization+WebSite+Service+MobileApplication graph, FAQPage) still json-parse; sitemap unchanged at 8 URLs (no route added). Follow-up: the dead-component list from 2026-08-05 (FareEstimator, EarningsCalculator, ReferralCallout, TopRides, Testimonials, PressLogos, BlogPosts) is still unaddressed; a Google Business Profile for the Maps pack is still outstanding; next content-phase candidate is a 6th city page if wer2 GO genuinely expands service, otherwise focus back on off-page (backlinks).
- 2026-08-10: Full site-wide technical/on-page audit (prompted by a broad external "master SEO prompt" — scoped down after confirming with Samuel: kept the current URL structure, skipped LocalBusiness/NAP schema and multi-language content since neither can be done without fabricating data). Ran an Explore-agent audit across 11 categories (images/alt, headings, metadata, JSON-LD, internal linking, dead links, dead components, brand consistency, URL hygiene, accessibility, robots/sitemap), then fixed every concrete finding: (1) Navbar's Home/How/Why anchors were dead on 7 of 8 pages (`#home` etc. only exist on `/`) — now route-aware `/#home` style hrefs that still same-document-scroll from the homepage itself; FAQ nav link stays a same-page `#faq` on `/`, `/drive` and city pages (each has its own FAQ) and only becomes `/#faq` on `/privacy-policy`, which has none. (2) Footer's "Press & news" link and the Independence Day promo's "How to enter" link both pointed at ids that don't exist anywhere (`#press`, `#promo`) — removed the dead Press link, repointed the promo CTA to the existing WhatsApp support link. (3) `/drive`, all 5 city pages and `/privacy-policy` were missing `og:site_name`/`og:locale` (Next doesn't merge `openGraph` objects across layout→page); `/privacy-policy` had no `twitter` card at all; city-page social titles lacked the brand suffix `/drive` already uses — all fixed. (4) Port Harcourt's rendered `<title>` was 66 chars (over the ~60 SERP cutoff) — shortened to 57; 4 of 5 city meta descriptions exceeded 160 chars — trimmed all to ≤160 without losing real local detail. (5) City-page `Service.provider` JSON-LD emitted a second unlinked anonymous `Organization` node instead of referencing the one canonical `@id` from the root layout (matching what `/drive` already does correctly) — fixed, consolidating the entity graph. (6) 4 hardcoded values that bypassed existing `lib/site.ts` constants (`Footer.tsx` driver URL + email, `privacy-policy/page.tsx` email + missing the import entirely, `DriveWithUs.tsx` QR caption) — all now source from the constants. (7) A heading-depth violation in the privacy policy (2.1.1/2.1.2 rendered as H3 siblings of their own H3 parent) — added an H4 helper and fixed nesting. (8) Deleted the 8 components flagged dead since 2026-08-05 (FareEstimator, EarningsCalculator, ReferralCallout, TopRides, Testimonials, PressLogos, BlogPosts, plus WhatsAppFab found this pass) — confirmed zero imports via repo grep first — and removed the `next.config.mjs` image-remote allowlist (ui-avatars.com/picsum.photos/images.unsplash.com) that only those components used. (9) Removed a pointless `robots.ts` disallow rule for `/api/`, a path that doesn't exist in this project. Deliberately NOT done and logged as such in `SEO_AUDIT.md`: URL restructuring (would break already-indexed URLs for no gain at 8 pages), LocalBusiness/NAP schema (no real address to publish), Hausa/Yoruba/Igbo content (no vetted translations exist), blog CMS/split sitemaps/programmatic pages/hreflang (over-engineering at this size, some explicitly against the brief's own anti-doorway-page rule). `npm run build` clean (0 errors, 1 pre-existing unrelated warning). Validated in built HTML: Abuja/PH titles ≤60 chars with brand suffix, og:site_name/locale present on all 3 previously-missing pages, both JSON-LD blocks per page json-parse, city Service.provider now uses the shared `@id`. Also verified live in a dev server via `document.querySelectorAll` on rendered Navbar hrefs: `/privacy-policy` shows `/#faq` (correct, no local section), `/lagos` shows `#faq` (correct, has one) — confirms the route-aware fix works, not just compiles. Full before/after table in `SEO_AUDIT.md`. Follow-up: Google Business Profile still outstanding; if a real (non-machine) Hausa/Yoruba/Igbo translation ever becomes available, add hreflang variants then, not before.
- 2026-08-12: Content-quality fix (rotating away from the last two technical-audit runs) — grepped every `body:` copy string across `components/` and `app/` and found one clear outlier: `components/HowItWorks.tsx`'s middle "How it works" step ("See your fare estimate") had body text of just `"Fair pricing."` (13 chars) while its two sibling steps run 86-100 chars of full, locale-specific sentences ("Open the app and pick a pickup spot — works on small streets in Surulere, Bompai or Asokoro alike." / "Verified through NIN and BVN. You see the driver, plate and rating before they arrive."). It read as unfinished stub copy on a homepage section every visitor sees. Rewrote to "Your fare shows before you confirm — based on real road distance, so there's no surprise when you arrive." — matching the exact upfront-estimate/no-surprise phrasing already used sitewide (WhyChooseUs, all 5 city-page FAQs, `/drive`), so it's consistent messaging, not new claims. Confirmed no other `body:` string site-wide is similarly thin (next-shortest non-stat-card body is 45 chars, and those short ones are deliberate perk-card micro-copy, not stubs). `npm run build` clean (0 errors, only the pre-existing unrelated Hero.tsx `exhaustive-deps` warning); verified the new string renders in the built `.next/server/app/index.html`. No route/route count change, so sitemap and JSON-LD were untouched by this edit. Live GSC check (screenshot from a connected session): `/privacy-policy` sits in "Discovered - currently not indexed" since 2026-08-05 — expected/low-priority for a policy page, not a regression, no action taken. Follow-up: Google Business Profile still outstanding; homepage H1 "Tap. Ride. Arrive." remains brand-y with zero keywords (title tag carries them) — a copy-testing candidate, not urgent; consider a 6th city page only if wer2 GO genuinely expands service.
- 2026-08-16: Internal-linking fix (rotating away from the 08-10 technical audit and 08-12 body-copy run) — the sitewide `Footer` was the weakest node in the link graph. Two concrete gaps: (1) its "Become a driver" entry still pointed at the EXTERNAL `driver.gower2.com` app redirector, a leftover from before `/drive` existed — the 2026-08-03 run repointed the Navbar but missed the Footer, so the page built to rank for driver-intent queries had only 2 internal inbound links (Navbar + the homepage DriveWithUs link) while a sitewide slot sent that equity off-domain instead. Relabelled to "Drive with wer2 GO" → internal `/drive`; the driver-app download is not lost, `/drive` carries 8 `DRIVER_URL` CTAs of its own. (2) The Footer had no city links at all, so the 5 city pages were reachable only from the homepage `CitiesServed` section and from each other — `/drive` and `/privacy-policy` were dead ends toward the city pages, and `/privacy-policy` had zero. Added a "Cities we serve" strip above the copyright bar ("Ride in <City>" pills, sourced from `lib/cities.ts` CITIES so a 6th city is automatic), reusing the exact pill markup already proven on the city pages' "other cities" strip and honouring the 44px tap-target floor set by the earlier mobile passes. Net effect measured in the built HTML: every one of the 8 pages now links to all 5 cities and to `/drive`; `/privacy-policy` went from 0 → 5 city links; `driver.gower2.com` no longer appears on non-`/drive` pages. `npm run build` clean (0 errors, only the pre-existing unrelated Hero.tsx `exhaustive-deps` warning); all JSON-LD blocks on all 8 pages still json-parse (2 per page except `/privacy-policy`'s 1); sitemap unchanged at 8 URLs (no route added). ⚠️ Visual check was against the built HTML only — dev servers cannot be started in an unattended scheduled run — mitigated by reusing existing pill/spacing classes verbatim; spot-checked live after deploy. Follow-up: the Footer blurb claims wer2 GO serves "across every state", which contradicts `SITE_DESCRIPTION`'s "Live in Lagos, Abuja, Port Harcourt, Uyo and Kano" and the 5 city pages — needs a truth ruling from Samuel (is it nationwide coverage or those 5 cities?) before anyone rewrites it, so deliberately left untouched this run. Also still outstanding: Google Business Profile for the Maps pack; homepage H1 "Tap. Ride. Arrive." remains keyword-free (title tag carries them). GSC (browser was connected): `/sitemap.xml` resubmitted, "Sitemap submitted successfully" (prior submit Aug 12, Success, 8 discovered pages). Did NOT spend Request-Indexing quota — this is a link-only change and the sitemap lastmod covers it, per the task brief. 📈 Coverage now **7 of 8 pages indexed** (`/`, `/drive` and all 5 city pages are in), with exactly one holdout: `/privacy-policy`, still "Discovered - currently not indexed" (Google systems, validation Not Started) since 2026-08-05. Worth noting for the next run: weak internal linking is the textbook cause of "Discovered - currently not indexed", and this run took that page from 0 → 5 city links + a `/drive` link, so it is a clean natural experiment — **check next run whether the internal-link fix alone flipped `/privacy-policy` to indexed**; if it is still Discovered by ~2026-08-19, that is the moment to spend one Request-Indexing on it rather than now.
- 2026-08-16 (follow-up, same day): Truth fix — Samuel ruled that wer2 GO serves **those 5 cities only, not nationwide**, resolving the open question logged earlier today. Fixed the two claims that were outright false under that ruling: (1) the sitewide `Footer` blurb's "in-app SOS on every trip — across every state" → "— live in Abuja, Port Harcourt, Uyo, Kano and Lagos", built at module scope from `lib/cities.ts` CITIES (`CITY_PHRASE`) rather than hardcoded, so a 6th city updates the sentence automatically and the copy can never drift from the pages that actually exist; (2) `components/Hero.tsx`'s first slide `ariaLabel` "Tap, ride and arrive anywhere in Nigeria" → "…across our Nigerian cities" — same false coverage claim, and as an aria-label it was what screen-reader users heard. Grepped the whole of `app/` + `components/` for sibling claims; deliberately LEFT the softer "live across Nigeria" phrasings in `CitiesServed.tsx`, `Hero.tsx` pillLabel, `app/drive/page.tsx` and the Footer's "Driven across Nigeria" sign-off, plus the layout JSON-LD "On-demand rides across Nigeria" — each sits immediately next to an explicit list of the 5 cities, and "across Nigeria" describes where the company operates rather than promising every state. Flag for a future run if Samuel wants those tightened too. `npm run build` clean; verified in the built HTML that all 8 pages render the new city sentence and that zero occurrences of "every state" or "anywhere in Nigeria" remain anywhere in the output.
- 2026-08-16 (follow-up 2): Samuel asked for the remaining softer coverage phrasings to be tightened too, so the "5 cities only" ruling is now applied everywhere with no exceptions. Changed: (1) `CitiesServed.tsx` eyebrow "Live across Nigeria" → "Live in {CITIES.length} Nigerian cities"; (2) `Hero.tsx` first-slide `pillLabel` same change (added a `lib/cities.ts` CITIES import purely for the count); (3) `app/drive/page.tsx` "wer2 GO is live across Nigeria and growing" → "live in these {CITIES.length} cities and growing"; (4) the `Footer` copyright sign-off "Driven across Nigeria." → "On the road in Nigeria."; (5) the root-layout JSON-LD `Service.description` "On-demand rides across Nigeria…" → now generated from the SAME `CITIES` array its own `areaServed` uses, so the description and the structured area list can never disagree ("On-demand rides in Abuja, Port Harcourt, Uyo, Kano and Lagos with verified drivers, upfront fare estimates and in-app SOS."). Every count and city list on the site is now derived from a CITIES array rather than hardcoded, so adding a 6th city updates all of it automatically. `npm run build` clean (only the pre-existing Hero.tsx exhaustive-deps warning); verified in the built HTML that **zero** occurrences of "across Nigeria", "every state", "anywhere in Nigeria" or "nationwide" remain in any of the 8 pages, that all JSON-LD still json-parses, and that the Service.description renders the 5-city sentence. ⚠️ Follow-up worth doing: there are TWO city sources — `lib/site.ts` CITIES (string tuple, used by the layout JSON-LD) and `lib/cities.ts` CITIES (objects, generates the pages/sitemap/UI). They currently agree at 5 names, but a 6th city added to only one would desync the JSON-LD from the real pages; collapsing `lib/site.ts` CITIES into a derived `CITY_NAMES` from `lib/cities.ts` is a clean, safe next-run change.
- 2026-08-16 (follow-up 3): Removed the two-city-sources drift risk flagged earlier today. `lib/site.ts` CITIES was a second hand-maintained array of city names (feeding the root-layout JSON-LD `areaServed` + `lib/faqs.ts`) sitting alongside `lib/cities.ts` CITIES (objects; generates the city pages, sitemap entries and homepage links). They agreed at 5 names by luck, but a city added to one and not the other would have silently desynced the structured data from the pages that actually exist. `lib/site.ts` now DERIVES its list — `export const CITIES: readonly string[] = CITY_DATA.map(c => c.name)` importing from `./cities` (no cycle: `lib/cities.ts` imports nothing). Also collapsed three copies of the same list-formatting logic (`lib/faqs.ts`'s local `joinWithAnd`, `Footer.tsx`'s `CITY_PHRASE`, and the inline slice/join added to `app/layout.tsx` earlier today) into one exported `joinWithAnd` + a ready-made `CITIES_PHRASE` constant in `lib/site.ts`, now used by all three. Side effect: `SITE_DESCRIPTION` is generated too, so its city clause can no longer go stale — the rendered order changed from "Lagos, Abuja, Port Harcourt, Uyo and Kano" to the canonical `lib/cities.ts` order "Abuja, Port Harcourt, Uyo, Kano and Lagos" (155 chars, still under the ~160 SERP cutoff). ⚠️ NOTE FOR THE NEXT RUN THAT ADDS A CITY: because SITE_DESCRIPTION now grows with the list, `lib/site.ts` carries a build-time guard that THROWS if it exceeds 160 chars. This is deliberate — a loud build failure beats silently shipping a truncated snippet. **Verified by experiment, not assumption:** temporarily added a 6th city to `lib/cities.ts`, rebuilt, and the build failed with exactly "Error: SITE_DESCRIPTION is 163 chars (max 160). Shorten it — see lib/site.ts." — which both proves the derivation works end-to-end (site.ts picked the new city up with no edit) and shows the guard does its job; test city then reverted and the clean build re-verified. If you hit that error, shorten the FIXED wording in SITE_DESCRIPTION — never drop a city from the list. Validated: `npx tsc --noEmit` clean, `npm run build` clean, JSON-LD `areaServed` now provably matches the set of generated city pages 1:1, zero "across Nigeria"/"every state"/"nationwide" anywhere in the output.
- 2026-08-18: Truth fix — attempted the flagged follow-up (check whether `/privacy-policy` flipped to "indexed" after the 08-16 internal-linking fix) but **GSC was unreachable this run**: `search.console.google.com` returned a browser error page on every navigation attempt (fresh tab, reload, different URLs) even though the same Chrome session was confirmed logged in as the correct account via `myaccount.google.com` — a genuine site/frame error, not a missing-browser case, so no sitemap resubmit or Request-Indexing happened this run; worth retrying next time. Fell back to a manual code audit instead and found a real miss from the three 08-16 truth-fix passes: `components/FindCities.tsx`'s app-download section said "you're ready to ride anywhere from Lagos to Maiduguri" — wer2 GO does not serve Maiduguri (Borno State; not one of the 5 cities in `lib/cities.ts`), so this was a false coverage claim that survived the earlier sweep because it names a specific city instead of using a caught phrase like "across Nigeria" or "every state". Grepped the whole of `app/` + `components/` + `lib/` for Maiduguri and five other plausible-but-unserved Nigerian cities (Ibadan, Enugu, Kaduna, Benin City, Owerri, Warri, Jos) — zero other hits. Fixed by replacing the hardcoded fragment with the same derived `CITIES_PHRASE` constant (`lib/site.ts`) already used by the Footer, root-layout JSON-LD and `/drive` — so it reads "ready to ride in Abuja, Port Harcourt, Uyo, Kano and Lagos" today and updates itself if a 6th city is ever added, instead of being another hand-typed claim that can drift. `npm run build` clean (0 errors, only the pre-existing unrelated Hero.tsx `exhaustive-deps` warning); grepped the built `.next/server/app/index.html` and confirmed zero remaining occurrences of "Maiduguri" and the new sentence renders with the correct 5-city list. No route/route-count change, so sitemap untouched at 8 URLs. Follow-up: retry the GSC `/privacy-policy` indexing check next run since it couldn't be done today; Google Business Profile still outstanding; homepage H1 remains keyword-free (flagged repeatedly, still not urgent).
- 2026-08-19: Internal-linking fix (rotating away from the run of technical/truth fixes on 08-16 through 08-18) — the homepage FAQ (`components/Faq.tsx` / `lib/faqs.ts`) had zero links in any of its 6 answers, despite two answers being directly, topically on-point for pages built specifically to rank: the "How do I become a wer2 GO driver?" answer never linked to `/drive` (the dedicated driver-recruitment page — this was a stronger, more contextual link opportunity than the generic sitewide Navbar/Footer/DriveWithUs links it already had, since a reader asking exactly that question is the highest-intent placement on the whole site), and "Does wer2 GO have a women-only ride option?" never linked to `/kano`, the only city where Sannu (the women-only ride) is live. Added an optional `link?: { href, label }` field to the `Faq` type in `lib/faqs.ts` — kept deliberately separate from the `a` text field so the FAQPage JSON-LD `Answer.text` in `app/page.tsx` (which reads `f.a` directly) stays plain text with no markup, exactly as schema.org expects; `Faq.tsx` renders the link as a small arrow-CTA under the answer only when present, so the other 4 FAQs are unchanged. Wired: driver FAQ → `/drive` ("See how driving works"), women-only-ride FAQ → `/kano` ("See wer2 GO in Kano"). `npm run build` clean (0 errors, only the pre-existing unrelated Hero.tsx `exhaustive-deps` warning); validated in the built `.next/server/app/index.html` that both JSON-LD blocks still json-parse, that the FAQPage `Answer.text` values contain zero markup (`<a` absent) so the schema wasn't polluted, and that both new `href="/drive"`/`href="/kano"` links and their labels render. No new route, so sitemap unchanged at 8 URLs. GSC (browser was connected, no UI error this time): checked the flagged `/privacy-policy` indexing question — still exactly **1 not-indexed page / 7 indexed**, and URL Inspection confirmed it's still `/privacy-policy`, "Discovered - currently not indexed" (first detected 8/5/26, validation Not Started), unchanged since the 08-16 internal-linking fix despite 3 days passing. That matches the 08-16 run's own plan ("if still Discovered by ~08-19, spend one Request-Indexing on it rather than now") — today is that date, so used exactly one Request-Indexing on `https://ng.gower2.com/privacy-policy` ("Indexing requested", added to the priority crawl queue). Also resubmitted `/sitemap.xml` (prior read Aug 14, Success, 8 pages) to cover today's homepage link-only change via lastmod, without spending a second Request-Indexing slot on `/` itself since only internal links were added, no new visible text. Follow-up: still outstanding — Google Business Profile for the Maps pack; homepage H1 remains keyword-free (flagged repeatedly, still not urgent); next run should check whether `/privacy-policy` finally flipped to indexed after today's Request-Indexing.
